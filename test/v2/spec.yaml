openapi: "3.0.0"
info:
  version: 1.0.0
  title: test
paths:
  /ccc:
    post:
      requestBody:
        required: false
        content:
          image/png:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: OK
  /bbb:
    post:
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: array
              items:
                type: integer
      responses:
        '200':
          description: OK
  /aaa:
    post:
      parameters:
        - name: test
          in: query
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Pet'
      responses:
        '200':
          description: OK
    put:
      parameters:
        - name: test
          in: query
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Pet'
      responses:
        '200':
          description: OK
    get:
      responses:
        '200':
          description: A paged array of pets
          content:
            image/png:
              schema:
                type: string
                format: binary

components:
  schemas:
    Pet:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        tag:
          type: string